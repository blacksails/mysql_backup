# A sample Gemfile
source "https://rubygems.org"

# Used to get the database names
gem 'mysql2'

# Used to get password from cli without writing it plaintext
gem 'io-console'

# Used to generate/read config file
gem 'psych'

ruby '2.1.1'
